const $=s=>document.querySelector(s);const $$=s=>[...document.querySelectorAll(s)];
const toggle=$('.nav-toggle'),nav=$('#nav-links');toggle.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')==='true';toggle.setAttribute('aria-expanded',String(!open));nav.classList.toggle('open',!open)});$$('#nav-links a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');toggle.setAttribute('aria-expanded','false')}));
$('#year').textContent=new Date().getFullYear();
const reveals=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');reveals.unobserve(e.target)}}),{threshold:.1});$$('.reveal').forEach(e=>reveals.observe(e));
const sections=$$('main section[id]'),links=$$('#nav-links a');const navObserver=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){links.forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+e.target.id))}}),{rootMargin:'-35% 0px -55%'});sections.forEach(s=>navObserver.observe(s));
addEventListener('scroll',()=>{const h=document.documentElement;$('.scroll-progress').style.width=(h.scrollTop/(h.scrollHeight-h.clientHeight)*100)+'%'},{passive:true});
const roles=['Java Developer','Backend Engineer','Full-Stack Builder','Problem Solver'];let ri=0,ci=0,deleting=false;function typeRole(){const full=roles[ri];ci+=deleting?-1:1;$('#role-text').textContent=full.slice(0,ci);let delay=deleting?45:85;if(!deleting&&ci===full.length){deleting=true;delay=1400}else if(deleting&&ci===0){deleting=false;ri=(ri+1)%roles.length;delay=250}setTimeout(typeRole,delay)}setTimeout(typeRole,1000);
const canvas=$('#network'),ctx=canvas.getContext('2d');let particles=[];function resize(){const r=canvas.getBoundingClientRect(),d=Math.min(devicePixelRatio,2);canvas.width=r.width*d;canvas.height=r.height*d;ctx.setTransform(d,0,0,d,0,0);particles=Array.from({length:Math.min(70,Math.floor(r.width/18))},()=>({x:Math.random()*r.width,y:Math.random()*r.height,vx:(Math.random()-.5)*.22,vy:(Math.random()-.5)*.22}))}function draw(){const r=canvas.getBoundingClientRect();ctx.clearRect(0,0,r.width,r.height);for(const p of particles){p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>r.width)p.vx*=-1;if(p.y<0||p.y>r.height)p.vy*=-1;ctx.fillStyle='rgba(0,240,90,.88)';ctx.fillRect(p.x,p.y,2,2)}for(let i=0;i<particles.length;i++)for(let j=i+1;j<particles.length;j++){const a=particles[i],b=particles[j],d=Math.hypot(a.x-b.x,a.y-b.y);if(d<145){ctx.strokeStyle=`rgba(0,230,170,${.28*(1-d/145)})`;ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke()}}requestAnimationFrame(draw)}resize();addEventListener('resize',resize);if(!matchMedia('(prefers-reduced-motion: reduce)').matches)draw();
const safe=n=>Number.isFinite(+n)?+n:0;async function loadGitHub(){try{const [user,repos]=await Promise.all([fetch('https://api.github.com/users/aditya-anivesh').then(r=>r.json()),fetch('https://api.github.com/users/aditya-anivesh/repos?per_page=100&sort=updated').then(r=>r.json())]);if(!Array.isArray(repos))throw Error();$('#gh-repos').textContent=user.public_repos??'—';$('#gh-followers').textContent=user.followers??'—';$('#gh-following').textContent=user.following??'—';$('#gh-stars').textContent=repos.reduce((s,r)=>s+safe(r.stargazers_count),0);const counts={};repos.forEach(r=>{if(r.language)counts[r.language]=(counts[r.language]||0)+1});const langs=Object.entries(counts).sort((a,b)=>b[1]-a[1]).slice(0,4),total=langs.reduce((s,x)=>s+x[1],0)||1;const colors=['#26e6ff','#ff9d00','#bd62ff','#00f05a'];$('#language-bar').innerHTML=langs.map((x,i)=>`<i style="width:${x[1]/total*100}%;background:${colors[i]}"></i>`).join('');$('#language-legend').innerHTML=langs.map((x,i)=>`<span><b style="color:${colors[i]}">●</b> ${x[0]} ${Math.round(x[1]/total*100)}%</span>`).join('');$('#gh-status').textContent='Live from GitHub';$('#repo-list').innerHTML=repos.filter(r=>!r.fork).slice(0,3).map(r=>`<a href="${r.html_url}" target="_blank" rel="noreferrer"><b>${r.name}</b><span>★ ${r.stargazers_count}</span></a>`).join('')||'<p>No public repositories found.</p>'}catch(e){$('#gh-status').textContent='Profile snapshot';$('#repo-list').innerHTML='<p>Open GitHub to view current repositories.</p>'}}loadGitHub();
async function loadLeetCode(){try{const d=await fetch('https://leetcode-api-faisalshohag.vercel.app/adityaanivesh').then(r=>{if(!r.ok)throw Error();return r.json()});const total=safe(d.totalSolved),easy=safe(d.easySolved),medium=safe(d.mediumSolved),hard=safe(d.hardSolved);if(!total)throw Error();$('#lc-total').textContent=total;$('#easy-value').textContent=easy;$('#medium-value').textContent=medium;$('#hard-value').textContent=hard;$('#easy-bar').style.width=Math.min(100,easy/safe(d.totalEasy||easy)*100)+'%';$('#medium-bar').style.width=Math.min(100,medium/safe(d.totalMedium||medium)*100)+'%';$('#hard-bar').style.width=Math.min(100,hard/safe(d.totalHard||hard)*100)+'%';$('#solve-ring').style.background=`conic-gradient(var(--green) 0 ${Math.min(100,total/700*100)}%,#0d2817 0)`;$('#lc-status').innerHTML='<i></i> Live profile statistics loaded'}catch(e){$('#lc-status').innerHTML='<i></i> 200+ solved · profile snapshot';$('#easy-bar').style.width='52%';$('#medium-bar').style.width='37%';$('#hard-bar').style.width='18%'}}loadLeetCode();
